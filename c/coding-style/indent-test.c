int
main(int argc, char **argv)
{
    char *foo;                /* way after foo */
    char *bar;  /* after bar */
    int baz_ggggggggggtk; /* variale long */
    if (2 != argc)

        printf("here\n"); /* after the code */
}

int foo ()
{
    puts("hi");/* Comment withing indentation that goes out of thee file length unless it's skimmed to the first column. */
    puts("Hi");
    /* after code */
}
#if HAS_FOO
/*
 * The procedure bar is even less interesting. A long comment going away of the
 * length of the file
 *
 * paragraph within the comment now having another very big line to be cut by the indent program
 */
char *
#else /* NO FOO */
int *
#endif /* NO FOO */
bar ()
{
/* annoying comment */
  puts("Hello");
    if (foo) {
        print("%d", foo); }
    else {
        (int)print("no foo");
    }

    for (i = 0; i < foo; i++) {print("%d", i);}

    for(i = 0; i < foo; i++)
        ;
    do

    {print( "sth" )
} while( 0 );

switch(i)
     {
       case 0:
             break;
    case 1:
       {
      sizeof (int);
    }
    default:
         break;
  }

}

struct uu {
int u;
};

#ifdef FOO
#if HAS_BAR
#   define FOOBAR 1
#else
#   define FOOBAR 0
#endif
#endif
int the (aaaaaaaaaaa,bbbbbbbbb,ccccccccc,uuuuuuu,o,w)
    int aaaaaaaaaaa, int bbbbbbbbb, int ccccccccc, struct thrm uuuuuuu, char *o, char *w
{
    int e, f, t;
    char u;
}
int a()
{
clean1:

    print (arg1, arg2, arg3,   arg4, arg5, arg6, arg7 , arg8, arg9, arg10, arg11, arg12, arg14);
clean2:

if ((((i < 2
&&
k > 0)
|| p == 0)
&&
q == 1)
||
n = 0)
    print("");
if ((((i < 2
+
k > 0)
+ p == 0) +
q == 1) +
n = 0)
    print("");

}



/***********************************************************************
* bTree program functions for the elimineRepeat version.               *
* This version build the binary tree only from a list with unique      *
* values, if there's repeated values it ignores them.                  *
***********************************************************************/

// extra functions used
void dropNode(TreeNumber *nodeToDrop);
void elimineRepeatedNodes(TreeNumber *fristNode);
